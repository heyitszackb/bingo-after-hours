import {chromium} from '@playwright/test';import assert from 'node:assert/strict';import {newStage,openRewardShop} from '../game.js';
const browser=await chromium.launch({channel:'chrome'});try{const page=await browser.newPage({viewport:{width:390,height:844},reducedMotion:'reduce'}),errors=[];page.on('pageerror',e=>errors.push(e.message));await page.addInitScript(()=>{const s=sessionStorage.getItem('fixture');if(s){localStorage.setItem('binglatro.run.v1',s);sessionStorage.removeItem('fixture');}window.events=[];const animate=Element.prototype.animate;Element.prototype.animate=function(f,o){if(this.matches('.joker-card.scoring-card'))window.events.push({id:this.dataset.joker,payout:this.dataset.payout});return animate.call(this,f,o);};});await page.goto(process.env.GAME_URL||'http://localhost:5173');const restore=async s=>{await page.evaluate(s=>sessionStorage.setItem('fixture',JSON.stringify(s)),{...s,stamps:[...s.stamps],bag:[...s.bag]});await page.reload();await page.locator('#play-button').click();};const s=newStage(10,5,{}, {},['face-value','crowd:1','full-sweep:1']);for(const n of [2,5,9]){s.stamps.add(n);s.stampBalls[n]=n;s.stampValues[n]=n;s.bag.delete(n);}s.tileMultipliers={25:3};s.tileCopiers={24:true};s.offer=[20];s.destinations={20:13};await restore(s);await page.locator('#balls .ball:not([disabled])').waitFor();await page.locator('[data-joker="full-sweep:1"]').click();await page.locator('#activate-card').click();await page.waitForFunction(()=>document.querySelector('#score').textContent==='25'&&document.querySelector('#balls .ball:not([disabled])'));assert.deepEqual((await page.evaluate(()=>window.events)).filter(e=>e.id==='crowd:1').map(e=>e.payout),['+3','+3','+3']);const shop=newStage();shop.status='passed';shop.bonusPaid=true;openRewardShop(shop);shop.shopOffer.cards=['crowd','encore'];await restore(shop);await page.locator('#shop-screen').waitFor({state:'visible'});assert.equal(await page.locator('.shop-product').count(),4);await page.locator('#shop-crowd').click();await page.locator('#balls .ball:not([disabled])').waitFor();assert.ok(await page.evaluate(()=>JSON.parse(localStorage.getItem('binglatro.run.v1')).jokers.includes('crowd:1')));assert.deepEqual(errors,[]);console.log('Passed Crowd per-tile +3 activations, stamp exclusion and shop reward.');}finally{await browser.close();}
