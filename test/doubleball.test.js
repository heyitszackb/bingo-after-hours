import test from 'node:test';import assert from 'node:assert/strict';import {newStage,choose,ballValue} from '../game.js';
function setup(two=false){const s=newStage(10);s.plainRules=true;s.target=10000;s.items[26]='doubleball';s.collection.push(26);s.bag.add(26);s.nextItemId=27;if(two){s.items[27]='doubleball';s.collection.push(27);s.nextItemId=28;}for(const [tile,id] of [[1,1],[2,2],[3,3],[4,two?27:4]]){s.stamps.add(tile);s.stampBalls[tile]=id;s.stampValues[tile]=ballValue(s,id);s.bag.delete(id);}s.tileStamps[5]=['x2'];s.tileMultipliers[5]=2;s.offer=[26];s.destinations={26:5};return s;}
test('red x2 ball is zero on x2 stamp and doubles full bingo after tile scoring',()=>{const s=setup(),r=choose(s,26);assert.equal(r.points,20);const last=r.activations.at(-1);assert.equal(last.points,0);assert.equal(last.multiplier,2);assert.deepEqual(last.groupEnd,{subtotal:10,factor:2,total:20,sources:[5]});});
test('multiple x2 balls multiply together, and non-scoring balls do not boost another bingo',()=>{const s=setup(true);assert.equal(choose(s,26).points,24);const other=setup();other.items[26]=undefined;other.ballValues[26]=5;other.items[27]='doubleball';other.stamps.add(13);other.stampBalls[13]=27;other.stampValues[13]=null;assert.equal(choose(other,26).points,20);});
