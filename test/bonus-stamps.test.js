import test from 'node:test';import assert from 'node:assert/strict';import {newStage,choose,activateCard,ballValue,tileStampList} from '../game.js';import {buildDebugBag,bagRecipe} from '../debug-bag.js';
const make=()=>newStage(10,5,{}, {},['face-value','full-sweep:1'],buildDebugBag([{type:'plus10',value:null,count:1},{type:'plus50',value:null,count:1},{type:'plus100',value:null,count:1},{type:'number',value:20,count:1},{type:'number',value:1,count:1}]));const play=(s,id)=>{s.offer=[id];s.playableSpaces=undefined;s.destinations={[id]:13};return choose(s,id);};
test('bonus stamps consume, leave space open and add together before multiplying',()=>{const s=make();for(const id of [26,27,28]){assert.equal(ballValue(s,id),null);play(s,id);assert.equal(s.stamps.size,0);assert.ok(!s.collection.includes(id));}s.tileMultipliers[13]=3;play(s,20);const r=activateCard(s,'full-sweep:1');assert.equal(r.points,540);assert.equal(r.activations[0].stampBonus,160);assert.equal(s.stampValues[13],20);const next=newStage(10,5,{}, {},s.jokers,s);assert.deepEqual(tileStampList(next,13),['plus10','plus50','plus100']);});
test('bonus stamps score without a points card, do not copy on placement, and roundtrip through Debug',()=>{const s=make();s.jokers=['full-sweep:1'];s.tileCopiers[13]=true;const r=play(s,26);assert.equal(r.copied,null);play(s,20);assert.equal(activateCard(s,'full-sweep:1').points,10);assert.deepEqual(bagRecipe(make()).slice(0,3),[{type:'plus10',value:null,count:1},{type:'plus50',value:null,count:1},{type:'plus100',value:null,count:1}]);});
