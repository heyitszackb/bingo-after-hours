import test from 'node:test';import assert from 'node:assert/strict';import {newStage,choose,activateCard} from '../game.js';import {buildDebugBag} from '../debug-bag.js';
const make=()=>newStage(10,5,{}, {},['face-value','full-sweep:1'],buildDebugBag([{type:'question',value:null,count:2},{type:'number',value:20,count:1},{type:'plus10',value:null,count:1}]));
const place=(s,id,t)=>{s.offer=[id];s.destinations={[id]:t};return choose(s,id,t,()=>0);};
test('Question Mark swaps immediately on play, preserving physical identities and one play cost',()=>{const s=make();const r=place(s,26,1);assert.equal(r.swap.to,20);assert.equal(s.stampBalls[1],20);assert.ok(s.bag.has(26)&&!s.bag.has(20));assert.equal(s.collection.length,4);assert.equal(s.calls,16);assert.equal(s.played[20],1);assert.equal(place(s,27,2).swap,null);assert.equal(s.stampBalls[2],27);});
test('scoring never swaps Question Marks, and no eligible item leaves the question mark in place',()=>{const s=make();s.bag.delete(20);assert.equal(place(s,26,1).swap,null);s.bag.add(20);const r=activateCard(s,'full-sweep:1');assert.equal(r.points,0);assert.equal(s.stampBalls[1],26);assert.ok(s.bag.has(20));});
test('replacement seed retains its starting value and does not grow on its own placement',()=>{const s=newStage(10,5,{}, {},[],buildDebugBag([{type:'question',value:null,count:1},{type:'seed',value:48,count:1},{type:'number',value:5,count:1}]));place(s,5,2);const r=place(s,26,1);assert.equal(r.swap.to,27);assert.equal(s.stampValues[1],48);assert.equal(s.stampValues[2],5);assert.equal(r.valueChanges.length,0);});
