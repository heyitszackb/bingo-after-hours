import test from 'node:test';import assert from 'node:assert/strict';import {newStage,choose,activateCard,ballValue} from '../game.js';import {buildDebugBag} from '../debug-bag.js';
const make=()=>newStage(10,5,{}, {},['face-value','full-sweep:1'],buildDebugBag([{type:'question',value:null,count:2},{type:'number',value:20,count:1},{type:'plus10',value:null,count:1}]));
const place=(s,id,t)=>{s.offer=[id];s.destinations={[id]:t};return choose(s,id);};
test('Question Marks swap real items 1:1, never question marks or stamps; shortages stay unknown',()=>{const s=make();place(s,26,1);place(s,27,2);const before=s.collection.length,r=activateCard(s,'full-sweep:1',()=>0);assert.equal(s.collection.length,before);assert.equal(s.stampBalls[1],20);assert.equal(s.stampBalls[2],27);assert.ok(s.bag.has(26)&&!s.bag.has(20));assert.equal(r.points,20);assert.equal(r.activations.filter(a=>a.reveal).length,1);assert.equal(ballValue(s,27),null);});
test('reveals do not fire placement powers and preserve permanent modifiers',()=>{const s=newStage(10,5,{}, {},['face-value','full-sweep:1'],buildDebugBag([{type:'question',value:null,count:1},{type:'hundred',value:48,count:1},{type:'number',value:5,count:1}]));place(s,26,1);place(s,5,2);s.bag=new Set([27]);const r=activateCard(s,'full-sweep:1');assert.equal(r.points,53);assert.equal(s.stampValues[2],5);assert.equal(s.stampBalls[1],27);});
test('empty eligible bag leaves question marks intact; offered item is cleared if swapped onto board',()=>{const s=make();place(s,26,1);s.bag=new Set([27,28]);assert.equal(activateCard(s,'full-sweep:1').points,0);assert.equal(s.stampBalls[1],26);const other=make();place(other,26,1);other.offer=[20];other.destinations={20:5};activateCard(other,'full-sweep:1');assert.deepEqual(other.offer,[]);assert.deepEqual(other.destinations,{});});
