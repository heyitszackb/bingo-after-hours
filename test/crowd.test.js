import test from 'node:test';import assert from 'node:assert/strict';import {newStage,choose,activateCard} from '../game.js';
function board(jokers=['face-value','crowd:1','full-sweep:1']){const s=newStage(10,5,{}, {},jokers);for(const [tile,n] of [[1,20],[2,null],[13,-4]]){s.stamps.add(tile);s.stampBalls[tile]=tile;s.stampValues[tile]=n;}s.tileMultipliers={1:2,25:3};s.tileCopiers={1:true,24:true};return s;}
test('every scored item gains board occupancy, including numberless pieces; ink does not count',()=>{const s=board(),r=activateCard(s,'full-sweep:1');assert.deepEqual(r.activations.map(a=>a.contributions.map(c=>c.points)),[[20,3],[0,3],[-4,3]]);assert.deepEqual(r.activations.map(a=>a.points),[46,3,-1]);});
test('Crowd modifies scoring without creating a scoring condition; Encore repeats it',()=>{const s=board(['crowd:1']);s.offer=[5];s.destinations={5:5};assert.equal(choose(s,5).points,0);const r=activateCard(board(['encore:1','crowd:1','full-sweep:1']),'full-sweep:1');assert.deepEqual(r.activations[0].contributions.map(c=>c.points),[3,3]);assert.equal(r.points,24);});
test('Bingo counts all occupied board items, not only its line, including the newly played item',()=>{const s=newStage(10,5,{}, {},['bingo','crowd:1']);for(const t of [1,2,3,4,25]){s.stamps.add(t);s.stampBalls[t]=t;s.stampValues[t]=t;s.bag.delete(t);}s.offer=[5];s.destinations={5:5};const r=choose(s,5);assert.equal(r.points,30);assert.ok(r.activations.every(a=>a.contributions[0].points===6));});
