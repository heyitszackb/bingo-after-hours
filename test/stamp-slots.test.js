import test from 'node:test';import assert from 'node:assert/strict';import {newStage,choose,deal,tileStampList} from '../game.js';import {buildDebugBag} from '../debug-bag.js';
const make=()=>newStage(10,5,{}, {},undefined,buildDebugBag([{type:'copier',value:null,count:2},{type:'x2',value:null,count:3},{type:'number',value:20,count:1}]));const play=(s,n)=>{s.offer=[n];s.destinations={[n]:13};return choose(s,n);};
test('four individual stamps coexist; fifth rejected without spending, consumption or copying; balls still allowed',()=>{const s=make();play(s,26);let r=play(s,28);assert.equal(r.copied,null);assert.deepEqual(tileStampList(s,13),['copier','x2']);play(s,29);play(s,30);const calls=s.calls,collection=[...s.collection];assert.equal(play(s,27),null);assert.equal(s.calls,calls);assert.deepEqual(s.collection,collection);assert.equal(s.tileMultipliers[13],8);assert.ok(play(s,20));assert.equal(s.stampValues[13],20);assert.deepEqual(tileStampList(newStage(10,5,{}, {},s.jokers,s),13),['copier','x2','x2','x2']);});
test('Copiers do not copy newly placed stamps; full tile is excluded from stamp destinations',()=>{const s=make();play(s,26);play(s,27);const r=play(s,28);assert.equal(r.copies.length,0);assert.ok(r.copies.every(c=>s.items[c.id]==='x2'));play(s,29);s.bag=new Set([30]);for(let i=0;i<30;i++){deal(s);assert.notEqual(s.destinations[30],13);}});
